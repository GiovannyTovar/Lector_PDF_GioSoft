<resources>
    <!-- Tema base. La interfaz real la dibuja Compose (ui/theme/Theme.kt);
         este tema solo cubre el arranque de la Activity y el contexto que
         necesitan las vistas internas del visor de androidx.pdf. -->
    <style name="Theme.LectorPdf" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">?attr/colorSurface</item>
    </style>
</resources>
